node_modules
dist
dist-ssr
*.local

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# CSV data store (legacy — migrated into SQLite on first boot)
server/data/*.csv

# SQLite user database
server/data/*.db
server/data/*.db-*

# Private keys — never commit to a public repo
*.pfx
*.p12
*.key

# Logs
logs
*.log
npm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
